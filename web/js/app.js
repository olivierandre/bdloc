// Generated by CoffeeScript 1.8.0
var Button, Cart, Init, Label;

Init = (function() {
  function Init() {
    var button;
    button = new Button();
  }

  return Init;

})();

Button = (function() {
  function Button() {
    this.buttonCart();
  }

  Button.prototype.buttonCart = function() {
    var selector;
    selector = $("#cart");
    return selector.on("click", function() {
      var cart, label;
      label = new Label().getLabel(this);
      cart = new Cart();
      return cart.manageCart(selector, label);
    });
  };

  return Button;

})();

Cart = (function() {
  function Cart() {}

  Cart.prototype.manageCart = function(selector, label) {
    var cart, item, labelAdd, labelRemove;
    labelAdd = "ajouter au panier";
    labelRemove = "retirer du panier";
    cart = $('#panier');
    item = parseInt(cart.text());
    if (label.toLowerCase() === labelAdd) {
      selector.text(labelRemove);
      return cart.text(item += 1);
    } else {
      selector.text(labelAdd);
      return cart.text(item -= 1);
    }
  };

  return Cart;

})();

Label = (function() {
  function Label() {}

  Label.prototype.getLabel = function(selector) {
    return $(selector).text();
  };

  return Label;

})();

$(function() {
  var init;
  return init = new Init();
});
