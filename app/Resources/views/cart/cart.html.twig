{% extends ':layouts:base.html.twig' %}

{% block main_content %}
	<div class="container">
		<div class="row">
		{% for book in books %}
		<div class="col-xs-12" id="cart">
			<div class="bookCart">
				<p>{{ book.id }} - {{ book.title }}</p>
			</div>
		</div>
	</div>
</div>
	
{% endfor %}
{% endblock %}