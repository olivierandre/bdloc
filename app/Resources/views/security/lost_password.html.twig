{% extends ':layouts:base.html.twig' %}

{% block main_content %}
    <h1>Mot de perdu ? :-/</h1>

<div class="container">
		<div class="row">
			<div class="col-xs-6">
				{% for flashMessage in app.session.flashbag.get('message') %}
				
				<div class="alert alert-danger">
			        <p>{{ flashMessage }}</p>
			    </div>
				{% endfor %}
				{{ form_start(lostForm, {'attr': {'novalidate' : 'novalidate'}}) }}

				{{ form_end(lostForm)}}

			</div>
		</div>
	</div>

{% endblock %}